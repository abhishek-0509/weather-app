Weather App Backend

This is a Spring Boot backend for a weather application.

Features:
- Get weather by city
- Get weather by location
- 5-day forecast
- Uses OpenWeather API

To run:
1. Add your API key in application.properties
2. Run: .\mvnw.cmd spring-boot:run
